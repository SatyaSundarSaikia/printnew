const scaleSelect = document.getElementById('scale'); // Define scaleSelect here

exportButton.addEventListener(
    'click',
    const selectedScale = parseFloat(scaleSelect.value);
    const scaleDenominator = 1 / selectedScale;